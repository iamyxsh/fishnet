services:
  fishnet:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fishnet
    ports:
      - "8473:8473"
    volumes:
      - fishnet-data:/home/fishnet/.fishnet
    restart: unless-stopped

volumes:
  fishnet-data:
